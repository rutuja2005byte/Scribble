NODE_ENV=production
PORT=4000

# Optional. In production, leave this empty when the frontend is served by Express.
# Set it only if your frontend and backend are deployed on different origins.
CLIENT_ORIGIN=

# Optional. For local development with a separate backend URL:
# VITE_SERVER_URL=http://localhost:4000
